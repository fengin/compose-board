# ComposeBoard 变更日志

本文件记录对用户、部署人员和维护者有影响的正式版本变化。

## [v1.2.0] - 2026-08-24

v1.2.0 在 v1.1.3 的 Docker 控制台日志能力上增加宿主机文件日志，原控制台日志、服务管理、环境配置、升级和终端流程保持兼容。文件日志默认关闭，只有配置安全基准目录后才显示入口。

### 新增

- 日志页增加“容器控制台 / 文件日志”来源选择，默认仍使用容器控制台。
- 文件日志支持服务级目录发现、目录和文件选择、SSE 实时跟随、文件轮转续接、普通日志下载和 `.gz` 归档下载。
- 下载接口支持 HTTP Range，适合下载较大的日志和压缩归档。
- 增加项目级 `.composeboard-file-logs.json`，保存 Compose service、稳定 `base_id` 和相对路径，可随相同目录结构的项目复制。
- 增加人工目录设置弹窗，支持安全基准目录下按层懒加载浏览、相对路径输入、检测、保存和恢复自动匹配。
- 增加 `file_logs` 配置和 `/api/file-logs/*` API。
- GitHub `v*` tag Release 工作流升级到 Go 1.25，发布 Linux/Windows/macOS 的 amd64/arm64 资产并生成 `SHA256SUMS`。

### 交互调整

- 日志来源、服务、目录、文件和操作按钮保持在同一工具栏，减少纵向空间占用。
- 服务、目录和文件选择框使用紧凑宽度，日志内容区保持主要页面高度。
- 控制台和文件日志共用服务排序规则：`backend` 服务端、`base` 中间件、`frontend` 前端，其余分类最后显示。
- 分类内部以及未标记服务保持 Compose/API 原始相对顺序，不再按名称重新排序。
- 未匹配服务不再默认选择第一个候选目录，也不会展示其他服务的日志目录。

### 自动发现实现

- 只检查当前所选服务的 Docker 实际 Mounts 和 Compose volumes，不扫描整个 `DATA_ROOT`。
- 通过运行时日志路径、Compose 环境、镜像名、service name、容器名、日志语义变量和 `log/logs` 路径段进行通用评分，不硬编码具体产品名称。
- 默认限制为最深2层、最多2000项、最长300ms，达到任一预算立即停止并返回 `discovery_truncated`。
- 自动发现缓存默认60秒，签名包含 Compose 文件、`.env` 修改时间和容器 ID。
- 空的日志语义挂载也可以成为候选，应用首次生成日志后无需重新配置目录。
- 互为父子关系的主日志挂载和子日志挂载视为同一日志树，避免 Nacos 等嵌套挂载造成错误的多候选冲突。
- 只有唯一高可信日志树或明显更强的候选才自动选中；不相关的多个高可信目录仍要求人工确认。

### 文件跟随实现

- 使用 Go 文件 API 读取，不执行宿主机 `tail` 命令。
- 初始尾读按64 KiB 块反向扫描，最多回溯32 MiB。
- 每500ms检测文件追加、截断、替换和重新创建；同名轮转后自动续接。
- 单行上限1 MiB，SSE 每15秒发送 heartbeat。
- Windows 使用允许删除共享的只读句柄，兼容日志轮转替换。

### 安全

- Web 页面不能新增或修改绝对安全基准目录。
- 人工映射只接收 `base_id + relative_path`，拒绝绝对路径、`..`、符号链接和越界路径。
- 浏览、检测、列举、实时跟随、下载和保存时均重新校验真实路径。
- 只允许普通文件，实时跟随和下载分别使用扩展名白名单。
- 安装脚本只解析项目 `.env` 中安全的非根 `DATA_ROOT`；无法确定安全路径时保持文件日志关闭。

### 兼容性

- v1.1.3 配置可以直接启动 v1.2.0；缺少 `file_logs` 时文件日志关闭，界面行为与旧版本一致。
- Docker 控制台历史日志和 SSE 接口没有被替换或改变。
- 文件日志发现不要求项目增加 ComposeBoard labels；`com.composeboard.category` 仍只是可选的 UI 分类标签。
- 人工映射文件不存在时不会创建数据库或其他状态服务。

### 从 v1.1.3 升级

1. 备份旧二进制和 `config.yaml`。
2. 替换 v1.2.0 对应架构的二进制并核对 SHA-256。
3. 如需文件日志，在配置中增加 `file_logs.allowed_bases`；建议将项目非根 `DATA_ROOT` 固化为安全基准。
4. 重启 ComposeBoard。若 `jwt_secret` 为空，旧登录令牌会失效，需要重新登录。
5. 验证控制台日志、文件日志、下载、服务排序和 Web 终端。

升级 ComposeBoard 自身不会自动重建业务容器。若同时修改业务服务的日志挂载或日志路径变量，需要由运维人员另行执行 Compose 重建。

### 已知边界

- `.gz` 只支持下载，不支持在线解压预览或实时跟随。
- 文件名必须命中下载扩展名白名单；例如 `config.log.日期.1` 默认不会作为 `.log` 文件列出。
- 安全基准目录是授权边界，不代表独立磁盘。生产环境仍应确保 `DATA_ROOT` 实际挂载到目标数据盘。
- 当前仍是单机、单项目、单服务单容器视图。

### 验证

- Go 全量测试与 `go vet ./...`。
- 目录发现、嵌套挂载、空挂载、路径越界、符号链接、轮转、SSE、Range 和映射复制测试。
- 服务排序、环境变量编辑、服务升级和 i18n 前端行为测试。
- Linux amd64/arm64 交叉构建和测试服务器升级验证。

## [v1.1.3]

v1.1.3 是 v1.2.0 的兼容基线，已具备 Compose 声明态服务视图、服务生命周期管理、Profile、环境变量编辑、镜像升级、Docker 控制台日志和 Web 容器终端。v1.2.0 在这些能力上追加文件日志，没有改变其主要操作契约。
